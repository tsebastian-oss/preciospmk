import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

type Msg={role:"user"|"assistant";content:string};
type Candidate={brand:string;products:number;score:number;matchType?:string};
type Runtime={enabled?:boolean;model?:string|null;api_key?:string|null};
type Cluster={clusterId:string;category:string;format?:string|null;measureType:string;sizeBucket?:number|null;products:number;retailers:number;qualityScore:number;examples?:string[];basisHint?:string};

const norm=(v:string)=>v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"").trim();
const cleanMsgs=(v:unknown):Msg[]=>Array.isArray(v)?v.filter((x:any)=>x&&(x.role==="user"||x.role==="assistant")&&typeof x.content==="string").map((x:any)=>({role:x.role,content:x.content.trim().slice(0,2500)})).filter((x:Msg)=>x.content).slice(-10):[];
const outText=(p:any)=>typeof p?.output_text==="string"?p.output_text.trim():(p?.output??[]).flatMap((x:any)=>x?.content??[]).filter((x:any)=>x?.type==="output_text"&&typeof x.text==="string").map((x:any)=>x.text).join("\n").trim();
const modelList=(configured?:string|null)=>[configured,"gpt-5.6-sol","gpt-5.6","gpt-5.6-terra","gpt-5.6-luna","gpt-5.5","gpt-4.1"].filter((x,i,a):x is string=>Boolean(x)&&a.indexOf(x)===i);
function dedupe(v:unknown):Candidate[]{if(!Array.isArray(v))return[];const m=new Map<string,Candidate>();for(const r of v as any[]){if(!r||typeof r.brand!=="string")continue;const k=norm(r.brand);if(!k)continue;const c={brand:r.brand.trim(),products:Number(r.products??0),score:Number(r.score??0),matchType:typeof r.matchType==="string"?r.matchType:undefined};const o=m.get(k);if(!o||c.score>o.score||(c.score===o.score&&c.products>o.products))m.set(k,c);}return[...m.values()].sort((a,b)=>b.score-a.score||b.products-a.products).slice(0,18);}
async function askJson(apiKey:string,models:string[],instructions:string,input:any[],max:number){let last="";for(const model of models){const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{authorization:`Bearer ${apiKey}`,"content-type":"application/json"},body:JSON.stringify({model,instructions,input,max_output_tokens:max,store:false})});const raw=await r.json().catch(()=>({}));if(!r.ok){last=raw?.error?.message??`OpenAI ${r.status}`;if([400,403,404].includes(r.status)||/model|access|permission|unsupported|does not exist/i.test(last))continue;break;}const txt=outText(raw).replace(/^```json\s*/i,"").replace(/^```\s*/,"").replace(/```$/,"").trim();try{return{value:JSON.parse(txt),model,error:""};}catch{last="Respuesta JSON inválida";}}return{value:null,model:null as string|null,error:last};}

async function resolveBrand(client:any,config:Runtime,org:string,q:string,history:string,filters:any){const{data}=await client.rpc("enterprise_brand_resolver_candidates",{p_organization_id:org,p_query:q,p_retailer_type:typeof filters.retailerType==="string"?filters.retailerType:"all",p_supermarket:typeof filters.supermarket==="string"&&filters.supermarket?filters.supermarket:null,p_category:typeof filters.category==="string"&&filters.category?filters.category:null,p_limit:24});const c=dedupe(data?.candidates),fb=typeof filters.brand==="string"?filters.brand.trim():"";if(fb){const x=c.find(z=>norm(z.brand)===norm(fb));return{brand:x?.brand??fb,candidates:c,confidence:1,method:"filter"};}const strong=c[0];if(strong&&strong.score>=.97&&["exact","phrase","normalized_exact"].includes(strong.matchType??""))return{brand:strong.brand,candidates:c,confidence:strong.score,method:"database_exact"};if(config.enabled&&config.api_key&&c.length){const r=await askJson(config.api_key,modelList(config.model),["Identifica qué marca quiso mencionar el usuario.","Elige SOLO una marca de BRAND_CANDIDATES o null; nunca inventes marcas.","Tolera errores de tipeo, abreviaciones y escritura fonética.","Devuelve SOLO JSON: {\"brand\":\"nombre exacto o null\",\"confidence\":0.0,\"reason\":\"breve\"}."].join("\n"),[{role:"developer",content:`BRAND_CANDIDATES:\n${JSON.stringify(c)}`},{role:"user",content:`Pregunta: ${q}\nContexto: ${history||"(sin contexto)"}`}],180);const rq=typeof r.value?.brand==="string"?r.value.brand:"",chosen=c.find(z=>norm(z.brand)===norm(rq));if(chosen&&Number(r.value?.confidence??0)>=.45)return{brand:chosen.brand,candidates:c,confidence:Number(r.value?.confidence??chosen.score),method:"gpt_resolver",resolverModel:r.model};}if(strong&&strong.score>=.70)return{brand:strong.brand,candidates:c,confidence:strong.score,method:"database_fuzzy"};return{brand:null,candidates:c,confidence:strong?.score??0,method:"unresolved"};}

async function pricingLens(client:any,config:Runtime,org:string,brand:string,q:string,history:string,filters:any){const priceIntent=/precio|pricing|car[oa]|barat|posicion|compet|compar|brecha|premium|índice|indice/i.test(q);if(!priceIntent)return null;const{data:preflight,error}=await client.rpc("enterprise_price_map_preflight",{p_organization_id:org,p_brand:brand,p_question:q,p_retailer_type:typeof filters.retailerType==="string"?filters.retailerType:"all",p_supermarket:typeof filters.supermarket==="string"&&filters.supermarket?filters.supermarket:null,p_category:typeof filters.category==="string"&&filters.category?filters.category:null,p_stock:typeof filters.stock==="string"?filters.stock:"all"});if(error||!Array.isArray(preflight?.clusters)||!preflight.clusters.length)return null;const intent=preflight?.explicitIntent??{};let pool=(preflight.clusters as Cluster[]).filter(c=>(!intent.format||c.format===intent.format)&&(!intent.sizeBucket||Number(c.sizeBucket)===Number(intent.sizeBucket)));if(!pool.length)pool=preflight.clusters as Cluster[];let chosen=pool[0],plan:any={method:"deterministic",confidence:Number(chosen.qualityScore??0)/100,reason:"Mejor cluster compatible"};if(pool.length>1&&config.enabled&&config.api_key){const compact=pool.slice(0,12).map(c=>({clusterId:c.clusterId,category:c.category,format:c.format,measureType:c.measureType,sizeBucket:c.sizeBucket,products:c.products,retailers:c.retailers,qualityScore:c.qualityScore,examples:(c.examples??[]).slice(0,4)}));const r=await askJson(config.api_key,modelList(config.model),["Elige el cluster correcto para responder una pregunta de pricing de marca.","No calcules precios. Elige SOLO clusterId existente.","Prioriza intención explícita, misma necesidad/categoría, mismo formato/tamaño y representatividad. Una muestra grande pero heterogénea no equivale a una buena comparación.","Devuelve SOLO JSON: {\"clusterId\":\"...\",\"confidence\":0.0,\"reason\":\"breve\"}."].join("\n"),[{role:"developer",content:`INTENT:\n${JSON.stringify(intent)}\nCLUSTERS:\n${JSON.stringify(compact)}`},{role:"user",content:`Pregunta: ${q}\nContexto: ${history||"(sin contexto)"}`}],200);const found=pool.find(c=>String(c.clusterId)===String(r.value?.clusterId??""));if(found){chosen=found;plan={method:"gpt_planner",confidence:Number(r.value?.confidence??found.qualityScore/100),reason:String(r.value?.reason??""),model:r.model};}}
  const{data:ctx,error:ce}=await client.rpc("enterprise_ai_price_map_context_v4",{p_organization_id:org,p_brand:brand,p_category:chosen.category,p_format:chosen.format??null,p_measure_type:chosen.measureType,p_size_bucket:chosen.sizeBucket??null,p_retailer_type:typeof filters.retailerType==="string"?filters.retailerType:"all",p_supermarket:typeof filters.supermarket==="string"&&filters.supermarket?filters.supermarket:null,p_stock:typeof filters.stock==="string"?filters.stock:"all"});if(ce||!ctx?.found)return null;return{plan:{cluster:chosen,...plan},context:ctx};}

function fallback(ctx:any){const s=ctx?.current?.summary;if(!s)return"No encontré una base suficiente para analizar esa marca.";return`${ctx.brand}: veo ${s.skus??0} SKU en ${s.retailers??0} cadenas. ${ctx.quality?.overallPriceComparable?`La mediana del cluster es $${Number(s.medianPrice??0).toLocaleString("es-CL")}.`:"El ticket global mezcla formatos/tamaños; no lo usaría para decir si la marca es cara o barata."}`;}

Deno.serve(async(req:Request)=>{if(req.method!=="POST")return new Response("Method not allowed",{status:405});const auth=req.headers.get("authorization")??"",url=Deno.env.get("SUPABASE_URL")!,anon=Deno.env.get("SUPABASE_ANON_KEY")!,service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;try{const body=await req.json().catch(()=>({})),org=typeof body?.organizationId==="string"?body.organizationId:"",messages=cleanMsgs(body?.messages),q=[...messages].reverse().find(m=>m.role==="user")?.content??"";if(!org||!q)return Response.json({error:"Falta la organización o la pregunta."},{status:400});const user=createClient(url,anon,{global:{headers:{Authorization:auth}}});const{data:u,error:ue}=await user.auth.getUser();if(ue||!u.user)return Response.json({error:"No autorizado"},{status:401});const svc=createClient(url,service);const{data:cfg}=await svc.rpc("get_ai_runtime_config_service");const config=(cfg??{}) as Runtime,filters=body?.filters??{},history=messages.filter(m=>m.role==="user").slice(-4,-1).map(m=>m.content).join(" \n ");const resolution=await resolveBrand(user,config,org,q,history,filters);if(!resolution.brand)return Response.json({answer:"No pude identificar la marca con suficiente certeza.",brand:null,candidates:resolution.candidates,resolution,ai:false});const{data:ctx,error}=await user.rpc("enterprise_brand_intelligence_context_v3",{p_organization_id:org,p_brand:resolution.brand,p_retailer_type:typeof filters.retailerType==="string"?filters.retailerType:"all",p_supermarket:typeof filters.supermarket==="string"&&filters.supermarket?filters.supermarket:null,p_category:typeof filters.category==="string"&&filters.category?filters.category:null,p_stock:typeof filters.stock==="string"?filters.stock:"all",p_days:Number.isFinite(Number(filters.period))?Math.max(7,Math.min(90,Number(filters.period))):30});if(error||!ctx?.found)return Response.json({answer:"Encontré la marca, pero no pude construir un contexto de datos suficientemente sólido.",brand:resolution.brand,resolution,ai:false});const lens=await pricingLens(user,config,org,resolution.brand,q,history,filters);if(!config.enabled||!config.api_key)return Response.json({answer:fallback(ctx),brand:resolution.brand,data:ctx,pricingLens:lens,resolution,ai:false});const compact={brand:ctx.brand,current:ctx.current,trend:ctx.trend,quality:ctx.quality,scope:ctx.scope,pricingLens:lens?{plan:lens.plan,category:lens.context.category,format:lens.context.format,measureType:lens.context.measureType,sizeBucket:lens.context.sizeBucket,priceBasis:lens.context.priceBasis,quality:lens.context.quality,targetRetailers:lens.context.targetRetailers,points:(lens.context.points??[]).slice(0,14)}:null};const r=await askJson(config.api_key,modelList(config.model),["Eres MGP Brand Intelligence, analista senior de pricing y retail.","Habla como un colega experto, natural y directo. Responde primero lo preguntado.","Los cálculos vienen del motor determinístico. No recalcules ni inventes cifras.","CURRENT.summary.medianPrice es MEDIANA DEL TICKET SKU, no precio unitario normalizado. Si QUALITY.overallPriceComparable=false, está prohibido usar ese número para afirmar que la marca es cara/barata/premium.","Para posicionamiento de precio usa PRICING_LENS cuando exista. PRICE_BASIS define exactamente la unidad comparable.","Si PRICING_LENS.quality.level=low, NO entregues una conclusión de posicionamiento ni un índice como si fuera confiable. Explica que la muestra es heterogénea y pide una familia/formato/tamaño más específico.","Si PRICING_LENS.quality.level=medium, interpreta con cautela y menciona la limitación.","TREND.variationPct es la mediana del cambio porcentual por SKU estable, no variación del promedio del surtido.","QUALITY.dataScore mide cobertura/calidad de datos; QUALITY.priceScore mide si el ticket global es comparable. No los confundas.","Por cadena usa únicamente CURRENT.retailers o PRICING_LENS.targetRetailers. Si hay un solo SKU comparable en una cadena, dilo explícitamente.","No inventes ventas, market share, margen, volumen ni causalidades.","Devuelve SOLO JSON: {\"headline\":\"...\",\"summary\":\"...\",\"insights\":[{\"title\":\"...\",\"detail\":\"...\"}],\"actions\":[\"...\"]}.","summary 1-3 frases; insights 2-4; actions 0-3 solo si aportan."].join("\n"),[{role:"developer",content:`DATA_CONTEXT:\n${JSON.stringify(compact)}`},...messages],1200);const a=r.value;if(a&&typeof a.headline==="string"&&typeof a.summary==="string"&&Array.isArray(a.insights))return Response.json({answer:a.summary,analysis:{headline:a.headline,summary:a.summary,insights:a.insights.slice(0,4),actions:Array.isArray(a.actions)?a.actions.slice(0,3):[]},brand:resolution.brand,model:r.model,ai:true,data:ctx,pricingLens:lens,resolution});return Response.json({answer:fallback(ctx),brand:resolution.brand,data:ctx,pricingLens:lens,resolution,ai:false,warning:r.error||"No pude generar el análisis completo."});}catch(e){return Response.json({error:e instanceof Error?e.message:String(e)},{status:500});}});