# Unified Platform Frontend

Single-shell enterprise UI for all pricing, commercial, catalog, export and scraping views. Uses existing authenticated API routes and keeps filters consistent across modules.
